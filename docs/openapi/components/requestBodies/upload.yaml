description: 'Document upload'
required: true
content:
  multipart/form-data:
    schema:
      properties:
        file:
          type: string
          description: A PDF, TIFF, JPEG or PNG file to upload
          required: true
